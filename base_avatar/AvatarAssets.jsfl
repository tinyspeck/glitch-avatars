var doc = fl.getDocumentDOM();var libItems = doc.library.items;var libItemsNum = libItems.length;//var classesString = 'var '+doc.name.substr(0,doc.name.length-4)+'Classes = [';var classesString = 'var AvatarAssetsClasses = [';var classesNum = 0;var classes = [];var names = {};fl.outputPanel.clear();for(var i = 0 ; i < libItemsNum; i++){ if(libItems[i].linkageExportForAS){  classes[classesNum] = libItems[i].linkageClassName;  names[classes[classesNum]] = libItems[i].name;  if (names[classes[classesNum]].indexOf('/') > -1) {  	names[classes[classesNum]] = names[classes[classesNum]].split('/').reverse()[0];  }  classesNum++; }  }classes.sort();for(i = 0; i < classesNum; i++){ classesString += '\r\t"'+classes[i]+'",'; if (names[classes[i]] != classes[i]) {	 for (var s=0;s<40-classes[i].length;s++)  classesString += ' ';	 classesString += '// BAD ' 	 classesString += names[classes[i]]; } }classesString +='\r];'fl.clipCopyString(classesString);fl.trace(classesString);